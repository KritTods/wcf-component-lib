export enum IsActive {
	Y = "Y",
	N = "N",
}

export enum ADDRESS {
	TAMBOL = "tambol",
	AMPHUR = "amphur",
	PROVINCE = "province",
	POSTCODE = "postcode",
	POSTAL_NAME_BY_ID = "postalNameById",
}

export enum SelectMode {
	MULTIPLE = "multiple",
	TAGS = "tags",
}
